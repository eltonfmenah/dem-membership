
document.addEventListener('DOMContentLoaded', () => {
    const form = document.querySelector('form');
    form.addEventListener('submit', () => {
        alert('Thank you for signing up with Destiny Evangelism Movement!');
    });
});
